if utente['fine'] is not None and isinstance(utente['fine'], datetime) and utente['fine'].date() > date.today():
                        raise Forbidden(UtilityMessages.forbiddenUtenteAlreadyLoggedInError(username, 'in'))